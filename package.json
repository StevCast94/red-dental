{
  "name": "red-dental",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npm run build && cd ../frontend && npm install && npx vite build",
    "start": "cd backend && node setup-db.js && node dist/server.js"
  }
}
