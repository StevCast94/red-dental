{
  "name": "red-dental",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "node setup-db.js && cd backend && node dist/server.js"
  }
}
